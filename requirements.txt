streamlit>=1.34,<2.0
pandas>=2.0,<3.0
numpy>=1.24,<2.0
torch>=2.1,<3.0
scikit-learn>=1.3,<2.0
scipy>=1.11,<2.0
pymongo>=4.6,<5.0
python-dotenv>=1.0,<2.0

# Optional - only required if you configure real OTP email login
# (BREVO_API_KEY / SENDER_EMAIL / HOST_EMAILS in secrets). The app
# runs fine without this package if those secrets are absent.
sib-api-v3-sdk>=7.6,<8.0

# NOTE: the Gemini-generated natural-language playlist summary from the
# original app was replaced in this version by the static, citation-backed
# Research Evidence explanation panel (modules/evidence.py), so google-genai
# is not required. Re-add it yourself if you want to restore that panel.
