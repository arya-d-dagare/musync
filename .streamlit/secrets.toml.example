# Copy this file to .streamlit/secrets.toml for LOCAL runs, or paste its
# contents into "Settings -> Secrets" if deploying on Streamlit Community
# Cloud. Never commit a real secrets.toml — it's already in .gitignore.
#
# Every key is optional; see README.md section 4 for what each enables.

MONGO_URI = ""
BREVO_API_KEY = ""
SENDER_EMAIL = ""
HOST_EMAILS = ""
